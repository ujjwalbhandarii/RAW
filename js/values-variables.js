console.log("hy from vs terminal using node");

// camel casing is mostly perfered

// 1st character must be letter, underscore(_) or $
// vaiable name are case sensative
var myName = "ujjwal";
// (variable) (name) = (value)
console.log(myName);
